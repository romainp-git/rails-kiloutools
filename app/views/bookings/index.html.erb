<div class="container my-5 dashboard-renter py-5">
  <div class="dashboard-header d-flex justify-content-between align-items-center mb-5">
    <div>
      <h1 class="dashboard-title fw-bold">Mes reservations <span>👋</span></h1>
      <p class="dashboard-subtitle text-secondary fs-5">Suivez l'évolution de vos réservations</p>
    </div>
  </div>
  <% if @bookings.any? %>
        <%= render 'shared/filtered_bookings_list', booking: @bookings%>
    <% else %>
      <p>Aucune réservation pour le moment.</p>
  <% end %>
</div>
