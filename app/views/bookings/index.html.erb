<div class="container mt-5">
      <h1 class="mb-4">Réservations</h1>
      <p class="text-muted">Suivez l'évolution de vos réservations</p>
  <% if @bookings.any? %>
        <%= render 'shared/filtered_bookings_list', booking: @bookings%>
    <% else %>
      <p>Aucune réservation pour le moment.</p>
  <% end %>
</div>
